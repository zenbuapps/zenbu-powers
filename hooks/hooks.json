{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" session-start",
            "async": false
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" user-prompt-submit",
            "async": false
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "agent",
            "prompt": "你是 zenbu-powers plugin 的 Stop Hook 自動驗收觸發器（agent hook 觸發的 sub-agent，非 orchestrator）。\n\nHook input data: $ARGUMENTS\n\n## 執行流程\n\n1. **讀取規範**：用 Read 工具讀取本 plugin 的 `hooks/stop-hook-spec.md`（首選絕對路徑：`${CLAUDE_PLUGIN_ROOT}/hooks/stop-hook-spec.md`；若該變數未被 Claude Code 替換、你看到字面 `${CLAUDE_PLUGIN_ROOT}` 而 Read 失敗，改用 Glob 搜尋 `**/zenbu-powers/hooks/stop-hook-spec.md` 找到並讀取）。\n2. **嚴格遵守規範**：依規範的「執行步驟」章節（Step 1-8）逐步執行；依規範的「Decision JSON 輸出格式」章節決定最終輸出。\n3. **時限自我約束**：整段邏輯 240 秒上限（hook timeout 300 秒留 60 秒緩衝）。超時或任何錯誤 → 輸出 `{}` allow stop，**絕對不要 block** 主流程造成卡死。",
            "timeout": 300,
            "async": false
          }
        ]
      }
    ]
  }
}
