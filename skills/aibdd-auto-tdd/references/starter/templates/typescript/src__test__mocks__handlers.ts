import type { RequestHandler } from 'msw';

// Default handlers for happy-path scenarios.
// Import and re-export from existing MSW handlers if available,
// or define default handlers here.
export const handlers: RequestHandler[] = [
  // Add default API handlers here
];
